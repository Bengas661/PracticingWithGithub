# Create empty directory (it creates a folder)
mkdir PutaNameHere
cd PutaNameHere/

#Converts directory to repository (creates hidden directory that stores metadata required for it to function):
git init

git status

#creates and initializes a directory at the same time
git init Dirname
cd Dirname/


Gia idi uparxon project:
  Grigoro deksi klik mesa sto fakelo --> Open Git Bash here
  Alliws:  cd /c/Users/user/Desktop/Git

# shows directory
pwd


---------------------------------------------------------
#showing content
ls -lrt
ls -ls
ls -la

# 1 kaneis edit ena arxeio (index.html) meso notepad:
notepad index.html

# 2 tsekareis ti uparxei:
git status

# 3 Allages pou eginan (working directory VS staging area) = what changed but NOT added:
git diff "vasikes edoles.txt"

# 4 Sedning to staging area
git add .

# 5 Check the same to confirm:
git diff "vasikes edoles.txt"

# 6 (staging area VS last commit) Difference between the last commit and the files you have already added with git add. = what was added and WILL be committed
git diff --staged 
git diff --staged HEAD   (=exact changes since last commit)
git diff --staged HEAD~1
git diff --staged HEAD~2
git diff --staged HEAD~3

# 7 shows a compact history of commits
git log --oneline 

# 8 Commiting and add message to the log for the update
git commit -m "new updates"

git log --oneline 

# 9 Checking specific changes by adding one of the codes found in git log command. For example: 
git diff b2aaf1c
----------------------------------------------------------


q to quit -


#################################
REVERTING TO EARLIER COMMIT VERSIONS
#################################

# Checking earlier commits
git log --oneline

git log --oneline "practice_file.txt"

# That shows the content of the required file
cat "practice_file.txt"

# Reverting to version 1 of the file 
git checkout HEAD~1 practice_file.txt












